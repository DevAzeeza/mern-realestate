import express from "express";

const app = express(); //to create an application.through this you can use methods

app.listen(3000, () => {
  console.log("server is running on port 3000ergh");
}); //listening to port number 3000
